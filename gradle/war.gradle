apply plugin: "war"

bootWar {
    mainClassName = "com.example.blog.MicroserviceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
